# Input is Flickr30k train images with one deterministic caption per image.
model_path: /mnt/share/r50063443/HunyuanImage-3.0-Instruct-Distil
device: npu:0
dtype: bf16
seed: 20260822

distributed:
  backend: hccl

source:
  manifest_path: datasets/flickr30k/metadata.jsonl
  image_root: datasets/flickr30k/flickr30k-images
  target_count: 2000

image:
  height: 1024
  width: 1024

output:
  cache_dir: data/cache
  shard_size: 32
